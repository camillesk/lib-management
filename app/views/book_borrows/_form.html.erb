<%= form_with(model: book_borrow) do |form| %>
  <% if book_borrow.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_borrow.errors.count, "error") %> prohibited this book_borrow from being saved:</h2>

      <ul>
        <% book_borrow.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.label :due_date, style: "display: block" %>
    <%= form.datetime_field :due_date %>
  </div>

  <div>
    <%= form.label :returned, style: "display: block" %>
    <%= form.check_box :returned %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
